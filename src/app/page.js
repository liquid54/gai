'use client'

import HeyGenAvatar from "./heyGenAvatar";  // Змінюємо на оригінальну назву компонента

const Home = () => {
    return <HeyGenAvatar />;  // Використовуємо ту ж назву, що й при імпорті
}

export default Home;